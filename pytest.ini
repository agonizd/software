[pytest]
# hair_system 项目 pytest 配置
# test_generator.py 是独立测试脚本（含 sys.exit()），不适用 pytest 收集

# 忽略目录（不在其中递归搜索测试）
norecursedirs = venv .git uploaded_photos

# 命令行追加参数
addopts = -v --tb=short --ignore=style_report/tests/test_generator.py --ignore=venv --ignore=.git --ignore=uploaded_photos
