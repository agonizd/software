# ============================================================
# AI 发型顾问 — 完整依赖
# pip install -r requirements.txt
# ============================================================

# 核心运行时
streamlit>=1.28.0
opencv-python>=4.8.0
numpy>=1.24.3
Pillow>=10.0.0
mediapipe>=0.10.9
python-dotenv>=1.0.0

# LLM / Agent 模式（可选，Mock 模式不需要）
dashscope>=1.20.0
langchain>=0.1.0
langchain-community>=0.1.0

# 测试
pytest>=7.4.0
